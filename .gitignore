# Rust / Tauri
src-tauri/target/
src-tauri/gen/schemas/
**/*.rs.bk
Cargo.lock

# Node (frontend has no build step, so just precaution)
node_modules/
dist/

# Sidecar build artifacts (99MB Bun-compiled exe) — built locally with `bun build --compile`
sidecar/mydoo-parser.exe
src-tauri/binaries/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
